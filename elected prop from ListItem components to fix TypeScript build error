warning: in the working copy of 'new-nextjs-app/src/components/property/SearchAutocomplete.tsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/new-nextjs-app/src/components/property/SearchAutocomplete.tsx b/new-nextjs-app/src/components/property/SearchAutocomplete.tsx[m
[1mindex 3881fcef..1ec12292 100644[m
[1m--- a/new-nextjs-app/src/components/property/SearchAutocomplete.tsx[m
[1m+++ b/new-nextjs-app/src/components/property/SearchAutocomplete.tsx[m
[36m@@ -1,15 +1,8 @@[m
 'use client';[m
 [m
 import React, { useState, useEffect, useCallback, useRef } from 'react';[m
[31m-import { TextField, List, ListItem, ListItemText, Paper, Box, CircularProgress, Typography, Divider, Chip, Skeleton } from '@mui/material';[m
[31m-import { Search, LocationOn, Home, TrendingUp, History, Star, Place } from '@mui/icons-material';[m
[31m-[m
[31m-interface SuggestionItem {[m
[31m-  name: string;[m
[31m-  count?: number;[m
[31m-  city?: string;[m
[31m-  isPopular?: boolean;[m
[31m-}[m
[32m+[m[32mimport { TextField, List, ListItem, ListItemText, Paper, Box, CircularProgress, Typography, Divider, Chip } from '@mui/material';[m
[32m+[m[32mimport { Search, LocationOn, Home, TrendingUp, History, Star } from '@mui/icons-material';[m
 [m
 interface SearchAutocompleteProps {[m
   value: string;[m
[36m@@ -28,22 +21,17 @@[m [mconst SearchAutocomplete: React.FC<SearchAutocompleteProps> = ({[m
 }) => {[m
   const [suggestions, setSuggestions] = useState<string[]>([]);[m
   const [categorizedSuggestions, setCategorizedSuggestions] = useState<{[m
[31m-    cities?: SuggestionItem[];[m
[31m-    states?: SuggestionItem[];[m
[31m-    types?: SuggestionItem[];[m
[31m-    amenities?: SuggestionItem[];[m
[31m-    neighborhoods?: SuggestionItem[];[m
[32m+[m[32m    cities?: string[];[m
[32m+[m[32m    states?: string[];[m
[32m+[m[32m    types?: string[];[m
[32m+[m[32m    amenities?: string[];[m
     recent?: string[];[m
     popular?: string[];[m
[31m-    trending?: string[];[m
   }>({});[m
   const [showSuggestions, setShowSuggestions] = useState(false);[m
   const [loading, setLoading] = useState(false);[m
[31m-  const [selectedIndex, setSelectedIndex] = useState(-1);[m
   const blurTimeoutRef = useRef<NodeJS.Timeout | null>(null);[m
   const containerRef = useRef<HTMLDivElement>(null);[m
[31m-  const inputRef = useRef<HTMLInputElement>(null);[m
[31m-  const suggestionsListRef = useRef<HTMLDivElement>(null);[m
 [m
   // Get recent searches from localStorage[m
   const getRecentSearches = useCallback(() => {[m
[36m@@ -67,88 +55,51 @@[m [mconst SearchAutocomplete: React.FC<SearchAutocompleteProps> = ({[m
   }, [getRecentSearches]);[m
 [m
   const fetchSuggestions = useCallback(async (query: string) => {[m
[31m-    // Always open the dropdown while we are actively searching[m
[31m-    setShowSuggestions(true);[m
[31m-[m
     if (!query || query.length < 2) {[m
[31m-      // For short queries, clear remote suggestions but keep dropdown[m
[31m-      // so recent searches (when value is empty) can still show.[m
       setSuggestions([]);[m
[31m-      setCategorizedSuggestions({});[m
[31m-      setLoading(false);[m
[32m+[m[32m      setShowSuggestions(false);[m
       return;[m
     }[m
 [m
     try {[m
       setLoading(true);[m
[31m-[m
[31m-      let response: Response | undefined;[m
[31m-[m
[31m-      // 1) Try the Next.js API route first[m
[32m+[m[32m      // Try Next.js API route first, fallback to backend API[m
[32m+[m[32m      let response;[m
       try {[m
[31m-        response = await fetch([m
[31m-          `/api/properties/search-suggestions?q=${encodeURIComponent(query)}&limit=10`[m
[31m-        );[m
[31m-[m
[31m-        // If the Next.js route exists but returns a non-OK status (e.g. 404 in production),[m
[31m-        // immediately fall back to the backend API.[m
[31m-        if (!response.ok) {[m
[31m-          console.warn([m
[31m-            'Search suggestions Next.js route returned',[m
[31m-            response.status,[m
[31m-            '- falling back to /api/v1/properties/search-suggestions'[m
[31m-          );[m
[31m-          response = await fetch([m
[31m-            `/api/v1/properties/search-suggestions?q=${encodeURIComponent(query)}&limit=10`[m
[31m-          );[m
[31m-        }[m
[32m+[m[32m        response = await fetch(`/api/properties/search-suggestions?q=${encodeURIComponent(query)}&limit=10`);[m
       } catch (e) {[m
[31m-        // 2) If calling the Next.js route itself fails (network/runtime),[m
[31m-        // fall back to the backend API.[m
[31m-        console.warn([m
[31m-          'Error calling /api/properties/search-suggestions, falling back to /api/v1/properties/search-suggestions',[m
[31m-          e[m
[31m-        );[m
[31m-        response = await fetch([m
[31m-          `/api/v1/properties/search-suggestions?q=${encodeURIComponent(query)}&limit=10`[m
[31m-        );[m
[32m+[m[32m        // Fallback to backend API[m
[32m+[m[32m        response = await fetch(`/api/v1/properties/search-suggestions?q=${encodeURIComponent(query)}&limit=10`);[m
       }[m
[31m-[m
[31m-      if (!response || !response.ok) {[m
[31m-        throw new Error(`HTTP error! status: ${response ? response.status : 'no response'}`);[m
[32m+[m[41m      [m
[32m+[m[32m      if (!response.ok) {[m
[32m+[m[32m        throw new Error(`HTTP error! status: ${response.status}`);[m
       }[m
[31m-[m
[32m+[m[41m      [m
       const data = await response.json();[m
[31m-[m
[31m-      // Handle both old format (array) and new format (categorized with counts)[m
[32m+[m[41m      [m
[32m+[m[32m      // Handle both old format (array) and new format (categorized)[m
       if (data.suggestions && Array.isArray(data.suggestions)) {[m
         setSuggestions(data.suggestions);[m
         setCategorizedSuggestions({});[m
       } else if (data.cities || data.states || data.types || data.amenities) {[m
[31m-        // New format with counts and additional data[m
         setCategorizedSuggestions({[m
           cities: data.cities || [],[m
           states: data.states || [],[m
           types: data.types || [],[m
[31m-          amenities: data.amenities || [],[m
[31m-          neighborhoods: data.neighborhoods || [],[m
[31m-          popular: data.popular || [],[m
[31m-          trending: data.trending || [][m
[32m+[m[32m          amenities: data.amenities || [][m
         });[m
         setSuggestions([]);[m
       } else {[m
         setSuggestions(data.suggestions || []);[m
         setCategorizedSuggestions({});[m
       }[m
[31m-[m
[31m-      // Keep dropdown open to show whatever suggestions we have[m
[32m+[m[41m      [m
       setShowSuggestions(true);[m
     } catch (error) {[m
       console.error('Error fetching suggestions:', error);[m
       setSuggestions([]);[m
[31m-      // Keep the dropdown open so the user at least sees an empty state[m
[31m-      // instead of it silently disappearing on errors.[m
[31m-      setShowSuggestions(true);[m
[32m+[m[32m      setShowSuggestions(false);[m
     } finally {[m
       setLoading(false);[m
     }[m
[36m@@ -190,116 +141,28 @@[m [mconst SearchAutocomplete: React.FC<SearchAutocompleteProps> = ({[m
       clearTimeout(blurTimeoutRef.current);[m
       blurTimeoutRef.current = null;[m
     }[m
[31m-    // Always show dropdown on focus; its contents (recent searches,[m
[31m-    // loading spinner, or suggestions) are controlled separately.[m
[31m-    setShowSuggestions(true);[m
[32m+[m[32m    if (value.length >= 2 && suggestions.length > 0) {[m
[32m+[m[32m      setShowSuggestions(true);[m
[32m+[m[32m    }[m
   };[m
 [m
   const handleBlur = () => {[m
     // Delay hiding suggestions to allow clicking on them[m
     blurTimeoutRef.current = setTimeout(() => {[m
       setShowSuggestions(false);[m
[31m-      setSelectedIndex(-1);[m
     }, 200);[m
   };[m
 [m
[31m-  // Highlight matching text in suggestion[m
[31m-  const highlightText = (text: string, query: string) => {[m
[31m-    if (!query || !text) return text;[m
[31m-    const regex = new RegExp(`(${query})`, 'gi');[m
[31m-    const parts = text.split(regex);[m
[31m-    return parts.map((part, index) => [m
[31m-      regex.test(part) ? ([m
[31m-        <span key={index} style={{ fontWeight: 600, color: 'var(--color-primary)' }}>[m
[31m-          {part}[m
[31m-        </span>[m
[31m-      ) : ([m
[31m-        <span key={index}>{part}</span>[m
[31m-      )[m
[31m-    );[m
[31m-  };[m
[31m-[m
[31m-  // Get all suggestions as a flat list for keyboard navigation[m
[31m-  const getAllSuggestions = useCallback(() => {[m
[31m-    const all: Array<{ text: string; type: string; count?: number }> = [];[m
[31m-    [m
[31m-    if (categorizedSuggestions.cities) {[m
[31m-      categorizedSuggestions.cities.forEach(city => {[m
[31m-        all.push({ text: city.name, type: 'city', count: city.count });[m
[31m-      });[m
[31m-    }[m
[31m-    if (categorizedSuggestions.states) {[m
[31m-      categorizedSuggestions.states.forEach(state => {[m
[31m-        all.push({ text: state.name, type: 'state', count: state.count });[m
[31m-      });[m
[31m-    }[m
[31m-    if (categorizedSuggestions.types) {[m
[31m-      categorizedSuggestions.types.forEach(type => {[m
[31m-        all.push({ text: type.name, type: 'type', count: type.count });[m
[31m-      });[m
[31m-    }[m
[31m-    if (categorizedSuggestions.amenities) {[m
[31m-      categorizedSuggestions.amenities.forEach(amenity => {[m
[31m-        all.push({ text: amenity.name, type: 'amenity', count: amenity.count });[m
[31m-      });[m
[31m-    }[m
[31m-    if (categorizedSuggestions.neighborhoods) {[m
[31m-      categorizedSuggestions.neighborhoods.forEach(neighborhood => {[m
[31m-        all.push({ text: neighborhood.name, type: 'neighborhood', count: neighborhood.count });[m
[31m-      });[m
[31m-    }[m
[31m-    if (categorizedSuggestions.popular) {[m
[31m-      categorizedSuggestions.popular.forEach(pop => {[m
[31m-        all.push({ text: pop, type: 'popular' });[m
[31m-      });[m
[31m-    }[m
[31m-    [m
[31m-    return all;[m
[31m-  }, [categorizedSuggestions]);[m
[31m-[m
[31m-  // Handle keyboard navigation[m
[31m-  const handleKeyDown = (e: React.KeyboardEvent) => {[m
[31m-    const allSuggestions = getAllSuggestions();[m
[31m-    const recentSearches = !value ? getRecentSearches() : [];[m
[31m-    const totalItems = allSuggestions.length + recentSearches.length;[m
[31m-[m
[31m-    if (e.key === 'ArrowDown') {[m
[31m-      e.preventDefault();[m
[31m-      setSelectedIndex(prev => (prev < totalItems - 1 ? prev + 1 : prev));[m
[31m-    } else if (e.key === 'ArrowUp') {[m
[31m-      e.preventDefault();[m
[31m-      setSelectedIndex(prev => (prev > 0 ? prev - 1 : -1));[m
[31m-    } else if (e.key === 'Enter' && selectedIndex >= 0) {[m
[31m-      e.preventDefault();[m
[31m-      if (selectedIndex < recentSearches.length) {[m
[31m-        handleSuggestionClick(recentSearches[selectedIndex]);[m
[31m-      } else {[m
[31m-        const suggestion = allSuggestions[selectedIndex - recentSearches.length];[m
[31m-        if (suggestion) {[m
[31m-          handleSuggestionClick(suggestion.text);[m
[31m-        }[m
[31m-      }[m
[31m-    } else if (e.key === 'Escape') {[m
[31m-      setShowSuggestions(false);[m
[31m-      setSelectedIndex(-1);[m
[31m-    }[m
[31m-  };[m
[31m-[m
   return ([m
     <Box ref={containerRef} sx={{ position: 'relative', width: '100%' }}>[m
       <TextField[m
[31m-        inputRef={inputRef}[m
         fullWidth[m
         variant="outlined"[m
         placeholder={placeholder}[m
         value={value}[m
[31m-        onChange={(e) => {[m
[31m-          onChange(e.target.value);[m
[31m-          setSelectedIndex(-1);[m
[31m-        }}[m
[32m+[m[32m        onChange={(e) => onChange(e.target.value)}[m
         onFocus={handleFocus}[m
         onBlur={handleBlur}[m
[31m-        onKeyDown={handleKeyDown}[m
         sx={{[m
           ...sx,[m
           '& .MuiOutlinedInput-root': {[m
[36m@@ -345,10 +208,8 @@[m [mconst SearchAutocomplete: React.FC<SearchAutocompleteProps> = ({[m
           onMouseDown={(e) => e.preventDefault()} // Prevent blur when clicking inside[m
         >[m
           {loading && suggestions.length === 0 && Object.keys(categorizedSuggestions).length === 0 ? ([m
[31m-            <Box sx={{ p: 2 }}>[m
[31m-              {[1, 2, 3].map((i) => ([m
[31m-                <Skeleton key={i} variant="text" width="100%" height={40} sx={{ mb: 1 }} />[m
[31m-              ))}[m
[32m+[m[32m            <Box sx={{ display: 'flex', justifyContent: 'center', alignItems: 'center', py: 2 }}>[m
[32m+[m[32m              <CircularProgress size={24} sx={{ color: 'var(--color-primary)' }} />[m
             </Box>[m
           ) : ([m
             <Box sx={{ p: 1 }}>[m
[36m@@ -366,18 +227,16 @@[m [mconst SearchAutocomplete: React.FC<SearchAutocompleteProps> = ({[m
                       key={`recent-${index}`}[m
                       component="div"[m
                       onClick={() => handleSuggestionClick(recent)}[m
[31m-                      selected={selectedIndex === index}[m
                       sx={{[m
                         py: 1,[m
                         px: 2,[m
                         cursor: 'pointer',[m
[31m-                        backgroundColor: selectedIndex === index ? 'var(--color-primary-light)' : 'transparent',[m
                         '&:hover': { backgroundColor: 'var(--color-primary-light)' }[m
                       }}[m
                     >[m
                       <History sx={{ fontSize: 18, mr: 1.5, color: 'var(--color-text-muted)' }} />[m
                       <ListItemText[m
[31m-                        primary={highlightText(recent, value)}[m
[32m+[m[32m                        primary={recent}[m
                         sx={{[m
                           '& .MuiListItemText-primary': {[m
                             color: 'var(--color-text-primary)',[m
[36m@@ -402,48 +261,30 @@[m [mconst SearchAutocomplete: React.FC<SearchAutocompleteProps> = ({[m
                           Cities[m
                         </Typography>[m
                       </Box>[m
[31m-                      {categorizedSuggestions.cities.map((city, index) => {[m
[31m-                        const itemIndex = getRecentSearches().length + index;[m
[31m-                        return ([m
[31m-                          <ListItem[m
[31m-                            key={`city-${index}`}[m
[31m-                            component="div"[m
[31m-                            onClick={() => handleSuggestionClick(city.name || city)}[m
[31m-                            selected={selectedIndex === itemIndex}[m
[32m+[m[32m                      {categorizedSuggestions.cities.map((city, index) => ([m
[32m+[m[32m                        <ListItem[m
[32m+[m[32m                          key={`city-${index}`}[m
[32m+[m[32m                          component="div"[m
[32m+[m[32m                          onClick={() => handleSuggestionClick(city)}[m
[32m+[m[32m                          sx={{[m
[32m+[m[32m                            py: 1,[m
[32m+[m[32m                            px: 2,[m
[32m+[m[32m                            cursor: 'pointer',[m
[32m+[m[32m                            '&:hover': { backgroundColor: 'var(--color-primary-light)' }[m
[32m+[m[32m                          }}[m
[32m+[m[32m                        >[m
[32m+[m[32m                          <LocationOn sx={{ fontSize: 18, mr: 1.5, color: '#4CAF50' }} />[m
[32m+[m[32m                          <ListItemText[m
[32m+[m[32m                            primary={city}[m
                             sx={{[m
[31m-                              py: 1,[m
[31m-                              px: 2,[m
[31m-                              cursor: 'pointer',[m
[31m-                              backgroundColor: selectedIndex === itemIndex ? 'var(--color-primary-light)' : 'transparent',[m
[