import React from 'react';
import { Input } from './Input';

export default {
  title: 'UI/Input',
  component: Input,
};

export const Default = {
  args: {
    placeholder: 'Type here...',
  },
};

